{
  "name": "openscreen-v2",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @openscreen/desktop dev",
    "dev:ext": "pnpm --filter @openscreen/extension dev",
    "build": "pnpm -r build",
    "build:desktop": "pnpm --filter @openscreen/desktop build",
    "build:extension": "pnpm --filter @openscreen/extension build",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.13",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
